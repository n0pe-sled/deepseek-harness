/** `settings.locale` namespace dictionary (the Language row's copy). */

/** The settings.locale namespace key union. */
export type SettingsLocaleKey = 'language.title'

/** English dictionary. */
export const en = {
  'language.title': 'Language',
} satisfies Record<string, string>
